{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pnpm db:embedding:reindex -- --dry-run --environment production --production-approved",
    "restartPolicyType": "NEVER"
  }
}
