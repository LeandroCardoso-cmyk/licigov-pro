# Cache de AST local e efêmero (atrelado à versão do graphify e à máquina).
cache/
# Snapshots datados que o graphify mantém a cada rebuild (histórico volumoso).
20*/
# Artefatos locais não versionados (visualização HTML e assinatura de labels).
graph.html
*.sig
